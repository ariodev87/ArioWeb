services:
  - type: web
    name: ariodev
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m reflex run --env prod --backend-only
    envVars:
      - key: PYTHON_VERSION
        value: 3.13.2